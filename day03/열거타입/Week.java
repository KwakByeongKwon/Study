package day03.열거타입;

public enum Week {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}

/**
 * 열거 타입은 한정된 값인 열거 상수 중에서 하나의 상수를 저장하는 타입입니다.
 * 위 의 열거 타입에서는 Week가 열거 타입의 이름이며 밑에 {} 안에 묶여있는 값들이 열거 상수 입니다.
 *
 * 열거 상수들은 모두 대문자로 작성하며 두개의 단어를 사용할경우 단어 사이에 _ 를 넣어줍니다.
 *
 * 같은 패키지 내의 파일에서 열거타입 변수를 선언하여 사용 가능
 */
